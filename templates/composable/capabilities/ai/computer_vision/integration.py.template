"""
Computer Vision Integration
===========================

APG capability-contract integration for Computer Vision.
This template is framework-neutral; transport, UI, and persistence adapters
are selected by the composing application.
"""

import logging
from importlib import import_module
from typing import Any


log = logging.getLogger(__name__)

CAPABILITY_CONTRACT: dict[str, Any] = {
    'name': 'Computer Vision',
    'category': 'ai',
    'version': '1.0.0',
    'features': ['Image Classification', 'Object Detection', 'Face Recognition', 'Image Segmentation', 'OCR Text Extraction', 'Video Processing', 'Real-time Analysis', 'Custom Model Training'],
    'models': ['Image', 'DetectionResult', 'CVModel', 'ProcessingJob'],
    'views': ['ImageView', 'DetectionView', 'ModelView'],
    'apis': ['cv/upload', 'cv/detect', 'cv/classify', 'cv/ocr'],
    'templates': ['cv_dashboard.html', 'image_gallery.html'],
    'static_files': [],
    'configuration': {'CV_UPLOAD_FOLDER': '/tmp/cv_uploads', 'CV_MAX_FILE_SIZE': 16777216, 'CV_ALLOWED_EXTENSIONS': ['jpg', 'jpeg', 'png', 'bmp', 'tiff']},
}


def integrate_computer_vision(application: Any = None, registry: Any = None, configuration: dict[str, Any] | None = None) -> dict[str, Any]:
    """Register Computer Vision metadata with an APG application or registry."""
    contract = dict(CAPABILITY_CONTRACT)
    contract['configuration'] = {
        **contract.get('configuration', {}),
        **(configuration or {}),
    }

    package_prefix = f"{__package__}." if __package__ else ""
    for module_name in ("models", "views"):
        try:
            import_module(f"{package_prefix}{module_name}")
        except ImportError as import_error:
            log.debug("Optional %s module %s not loaded: %s", 'Computer Vision', module_name, import_error)

    if registry is not None:
        if hasattr(registry, "register_capability"):
            registry.register_capability(contract)
        elif isinstance(registry, dict):
            registry[contract['name']] = contract

    if application is not None and hasattr(application, "capabilities"):
        application.capabilities[contract['name']] = contract

    log.info("Registered APG capability: %s", contract['name'])
    return contract


class ComputerVisionCapability:
    """Executable capability wrapper for Computer Vision."""

    def __init__(self, application: Any = None, registry: Any = None, configuration: dict[str, Any] | None = None):
        self.application = application
        self.registry = registry
        self.config = configuration or {}
        self.initialized = False
        self.contract: dict[str, Any] | None = None
        if application is not None or registry is not None:
            self.init_app(application, registry, configuration)

    def init_app(self, application: Any = None, registry: Any = None, configuration: dict[str, Any] | None = None) -> dict[str, Any]:
        self.application = application
        self.registry = registry
        self.config = configuration or self.config
        self.contract = integrate_computer_vision(application, registry, self.config)
        self.initialized = True
        return self.contract

    def get_status(self) -> dict[str, Any]:
        return {
            'initialized': self.initialized,
            'capability': 'Computer Vision',
            'configuration_keys': sorted(self.config),
        }
