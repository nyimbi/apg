# FREC Capability Specification Pointer

The authoritative FREC capability specification is maintained in `SPECIFICATION.md`.

This compatibility file remains for tooling that still looks for `cap_spec.md`. The current packet includes first-class facial-recognition governance-agent composition, Bytewax-first lifecycle batch validation, and governed production API image-source normalization for strict base64 payloads, data URLs, and public HTTP/HTTPS image URLs.
