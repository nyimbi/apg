# MFAU Capability Specification Pointer

The authoritative MFAU specification is maintained in `SPECIFICATION.md`.

This compatibility file remains for tooling that still looks for `cap_spec.md`. The current executable packet also includes first-class MFA security-agent composition and Bytewax lifecycle-batch validation.
