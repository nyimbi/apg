# DEPL Capability Specification Pointer

The active DEPL specification lives in `SPECIFICATION.md`.

This compatibility file remains for older tooling that still scans
`cap_spec.md`. Use `README.md` for operator usage, `SPECIFICATION.md` for the
normative capability contract, and `PLAN.md` for the implementation plan and
verification evidence expected for this packet.
