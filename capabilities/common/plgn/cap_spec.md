# Plugin/Extension Framework Capability Specification

The active PLGN specification now lives in `SPECIFICATION.md`.

This compatibility pointer remains for tooling that still discovers capability
directories through `cap_spec.md`.
