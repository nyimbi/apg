# DVRL Follow-Up Work

This file tracks deferred work after the current generated-application
lifecycle packet.

- Bind `DVRLVirtualizationAgentRecord` to real external runtime adapters for
  Codex, Claude Code, OpenCode, Pi, and future agent providers.
- Add rendered UI screens for the agent roster and Bytewax lifecycle batch
  monitor once the APG shell rendering layer is selected.
- Exercise live Bytewax topologies for source, schema, virtual table, query,
  cache, policy, and agent lifecycle batches.
- Add production connector smoke tests for selected database, warehouse,
  lakehouse, API, file, stream, and Singer tap source types.
- Run full repository tests, browser checks, and performance benchmarks in a
  dedicated verification window.
