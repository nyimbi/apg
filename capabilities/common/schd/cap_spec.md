# Scheduling and Job Orchestration Capability

The active capability specification is `SPECIFICATION.md`.

Keep this compatibility file as a short pointer for older tooling that still
discovers `cap_spec.md`; update `SPECIFICATION.md`, `PLAN.md`, and `README.md`
for capability behavior, runtime scope, adapter boundaries, and verification
commands.

The current packet adds first-class provider-neutral scheduler agents and
Bytewax lifecycle-batch guardrails on top of the existing calendar, worker,
job, schedule, run, retry, dead-letter, audit, UI, and theming runtime.
