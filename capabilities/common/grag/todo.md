# GRAG Follow-up Work

- Wire `grag_runtime.GragService` to production graph, vector, model, auth, audit, metrics, cache, and Bytewax adapters.
- Add persistent storage migrations for generated-app records if the APG compiler chooses a database-backed target.
- Add rendered browser verification for generated screens once the app shell consumes the GRAG view metadata.
- Add live graph/vector/model integration tests when battery and infrastructure budgets allow.
- Add retrieval-quality and reasoning-quality benchmarks after the executable lifecycle stabilizes.
