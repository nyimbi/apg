[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "apg-healthcare-reg"
version = "1.0.0"
description = "APG Healthcare Regulatory — healthcare domain capability"
readme = "README.md"
license = {text = "Proprietary"}
requires-python = ">=3.11"
keywords = ["apg", "datacraft", "capability", "healthcare", "reg"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.14",
    "Operating System :: OS Independent",
]
dependencies = [
        "pydantic>=2.0",
    "uuid6>=0.4",
    "sqlalchemy>=2.0",
    "flask>=3.0",
]

[project.optional-dependencies]
streaming = ["bytewax>=0.18"]
nlp       = ["transformers>=4.30", "sentence-transformers>=2.2"]
vision    = ["opencv-python>=4.0", "Pillow>=10.0"]
geo       = ["shapely>=2.0", "geopy>=2.4"]
crypto    = ["cryptography>=41.0"]
full      = [
    "apg-healthcare-reg[streaming]",
    "apg-healthcare-reg[nlp]",
    "apg-healthcare-reg[vision]",
    "apg-healthcare-reg[geo]",
    "apg-healthcare-reg[crypto]",
]

[project.urls]
Homepage   = "https://www.datacraft.co.ke"
Repository = "https://github.com/datacraft/apg"
Changelog  = "https://github.com/datacraft/apg/blob/main/capabilities/healthcare/reg/CHANGELOG.md"


[project.scripts]
apg-healthcare-reg = "apg_healthcare_reg.app:main"
[project.entry-points."apg.capabilities"]
healthcare_reg = "apg_healthcare_reg:get_capability_contract"

[tool.setuptools]
package-dir = {"apg_healthcare_reg" = "."}
packages    = ["apg_healthcare_reg"]

[tool.setuptools.package-data]
"apg_healthcare_reg" = ["py.typed", "README.md", "*.pyi"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
