[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "apg-hcm-payroll"
version = "2.2.0"
description = "APG Payroll Management — hcm domain capability"
readme = "README.md"
license = {text = "Proprietary"}
requires-python = ">=3.11"
keywords = ["apg", "datacraft", "capability", "hcm", "payroll"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.14",
    "Operating System :: OS Independent",
]
dependencies = [
        "pydantic>=2.0",
    "uuid6>=0.4",
    "sqlalchemy>=2.0",
    "flask>=3.0",
]

[project.optional-dependencies]
streaming = ["bytewax>=0.18"]
nlp       = ["transformers>=4.30", "sentence-transformers>=2.2"]
vision    = ["opencv-python>=4.0", "Pillow>=10.0"]
geo       = ["shapely>=2.0", "geopy>=2.4"]
crypto    = ["cryptography>=41.0"]
full      = [
    "apg-hcm-payroll[streaming]",
    "apg-hcm-payroll[nlp]",
    "apg-hcm-payroll[vision]",
    "apg-hcm-payroll[geo]",
    "apg-hcm-payroll[crypto]",
]

[project.urls]
Homepage   = "https://www.datacraft.co.ke"
Repository = "https://github.com/datacraft/apg"
Changelog  = "https://github.com/datacraft/apg/blob/main/capabilities/hcm/pay/payroll/CHANGELOG.md"


[project.scripts]
apg-hcm-payroll = "apg_hcm_payroll.app:main"
[project.entry-points."apg.capabilities"]
pay_payroll = "apg_hcm_payroll:get_capability_contract"

[tool.setuptools]
package-dir = {"apg_hcm_payroll" = "."}
packages    = ["apg_hcm_payroll"]

[tool.setuptools.package-data]
"apg_hcm_payroll" = ["py.typed", "README.md", "*.pyi"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
