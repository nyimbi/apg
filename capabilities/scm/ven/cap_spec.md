# Vendor Management Capability Spec

The active specification is maintained in `SPECIFICATION.md`.

This compatibility file keeps APG tooling and older discovery paths pointed at the same capability packet.
